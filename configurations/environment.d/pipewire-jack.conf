# PipeWire JACK library path configuration
# This allows JACK applications to use PipeWire's JACK implementation
# Note: This is a static template. apply.sh generates the actual file dynamically.
LD_LIBRARY_PATH=/usr/lib/x86_64-linux-gnu/pipewire-0.3/jack${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}
